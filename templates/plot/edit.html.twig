{% extends 'base.html.twig' %}

{% block title %}Modifier {{ plot.name }} — Carnet de semis{% endblock %}

{% block body %}
    <p class="card__meta"><a href="{{ path('app_plot_show', {id: plot.id}) }}">← {{ plot.name }}</a></p>
    <h1>Modifier l'emplacement</h1>

    {% include 'plot/_form.html.twig' with {bouton: 'Enregistrer'} %}
{% endblock %}
